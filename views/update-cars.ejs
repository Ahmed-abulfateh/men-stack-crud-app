<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Car</title>
</head>
<body>
    <h1>Update <%= carToUpdate.name %>   <%= carToUpdate._id %></h1>
    
    <form action="/cars/update/<%= carToUpdate._id %>" method="POST">
        <input type="text" name="name" value="<%= carToUpdate.name %>">
        
        <input type="checkbox" name="isReadyToDrive" 
               <%= carToUpdate.isReadyToDrive ? "checked" : "" %>> Ready to Drive?
        
        <button type="submit">Update Car</button>
    </form>
</body>
</html>
